
  # Payment App with Sharing Features

  This is a code bundle for Payment App with Sharing Features. The original project is available at https://www.figma.com/design/ENaNhHlubIsam7PzxUOki0/Payment-App-with-Sharing-Features.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  